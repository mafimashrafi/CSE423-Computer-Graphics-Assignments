    # mpl_algorithm = mid_point_line_drawing(dmd_common_x, dmd_common_y, dmd_common_x + 30, dmd_common_y)
    # pixel_list = mpl_algorithm.finding_pixels()
    # lets_draw_line = drawing_line(pixel_list)
    # lets_draw_line.draw_line()